{
  /*
       <View
        style={{
          height: 50,
          width: '100%',
          backgroundColor: 'red',
          flexDirection: 'row',
          alignItems: 'center',
        }}>
        {bottomControlerArr.map(item => {
          console.log('item', item);
          return (
            <TouchableOpacity onPress={() => {}}>
              <AppIcon
                type={Icons.AntDesign}
                name="chat"
                color={AppColors.black}
                size={20}
              />
            </TouchableOpacity>
          );
        })}
      </View> 
      */
}
